build
# The cross toolchain: gigabytes of vendor binaries, either unpacked here or
# symlinked to an install elsewhere. See mk/toolchain.mk. Both arrangements
# are ignored, so the symlink form is not mistaken for a file to commit.
/toolchains
/toolchains/
/scratchpad/
/media/
.DS_Store
